@using TraversalProject.EntityLayer.Concrete
@model Reservation
@{
    ViewData["Title"] = "Yeni Rezervasyon Oluştur";
    Layout = "~/Areas/Member/Views/Shared/_MemberLayout.cshtml";
}

<h1>Yeni Rezervasyon Oluştur</h1>

<form  method="post">
   @*  <div class="form-group">
        <label>Lokasyon</label>
        @Html.DropDownListFor(X => X.Destination, (List<SelectListItem>)ViewBag.v, new { @class = "form-control" })
    </div> *@

    <div class="form-group">
        <label>Kişi Sayısı</label>
       <input type="text" name="PersonCount" class="form-control"/>
    </div>

    <div class="form-group">
        <label>Tarih</label>
        <input type="date" name="ReservationDate" class="form-control" />
    </div>

    <div class="form-group">
        <label>Açıklama</label>
        <input type="text" name="Description" class="form-control" />
    </div>

    <button type="submit" class="btn btn-outline-success">Rezervasyon Oluştur</button>
</form>